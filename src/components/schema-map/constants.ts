/**
 * CSS pixel constants for node height calculation.
 * These MUST match the actual rendered layout in table-node.tsx.
 */
export const HEADER_HEIGHT = 75; // px-3 py-2, label + name + metadata row
export const BORDER_T = 1; // border-t on expanded section
export const GROUP_HEADER_H = 29; // py-1.5 + text-xs + border-b
export const GROUP_PAD = 4; // py-1 on expanded group container
export const FIELD_ROW_H = 20; // py-0.5 + text-xs
export const FILTER_INPUT_H = 28; // column filter input row height
export const MAX_VISIBLE_ROWS = 30;
